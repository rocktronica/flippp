<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <title>desiwalk</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        @page {
            margin: 0;
            padding: 0;

            size: {{page_width}} {{page_height}};
        }

        html {
            background: #ddd;
        }

        .page {
            width: {{page_width}};
            background: #fff;
            display: flex;
            flex-wrap: wrap;
            margin: .5in auto;
        }

        .panel {
            width: calc({{page_width}} / {{columns}});
            height: calc({{page_height}} / {{rows}});

            padding: .25in;

            outline: 1px dashed #ccc;

            display: flex;
            justify-content: space-between;
        }

        .handle {
            width: 1in;
            min-width: 1in;
            font-family: monospace;
            writing-mode: vertical-lr;
        }

        .content {
            flex: 1;

            background-size: contain;
            background-repeat: no-repeat;
            background-position: center center;

            filter: contrast(120%) brightness(200%);
        }

        @media print {
            html, .page {
                background: none !important;
            }

            .page {
                margin: 0;
            }
        }
    </style>
</head>

<body>
    {{#pages}}
        <div class="page">
            {{#panels}}
                <div class="panel">
                    {{#filename}}
                        <div class="handle">
                            {{filename}} | Page {{page}}
                        </div>
                        <div class="content" style="background-image:url({{filename}})"></div>
                    {{/filename}}
                </div>
            {{/panels}}
        </div>
    {{/pages}}
</body>

</html>
