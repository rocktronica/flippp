<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="utf-8" />
    <title>desiwalk</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        @page {
            margin: 0;
            padding: 0;

            size: 8.5in 11in;
        }

        html {
            background: #eee;
        }

        .page {
            width: 8.5in;
            background: #fff;
            display: flex;
            flex-wrap: wrap;
            margin: .25in auto;
        }

        .panel {
            width: calc(8.5in / {{columns}});
            height: calc(11in / {{rows}});

            padding: .25in;

            outline: 1px dashed #ccc;

            display: flex;
            justify-content: space-between;
        }

        .handle {
            width: .5in;
            min-width: .5in;
            font-family: monospace;
            writing-mode: vertical-lr;
        }

        .content {
            /* TODO: tidy, fix against portrait/landscape */
            width: calc((11in - .25in * {{rows}} * 2) / {{rows}});
            height: calc((11in - .25in * {{rows}} * 2) / {{rows}});

            background-size: cover;
            background-repeat: no-repeat;
            background-position: center center;

            filter: contrast(120%) brightness(200%);
        }

        @media print {
            html, .page {
                background: none !important;
            }

            .page {
                margin: 0;
            }
        }
    </style>
</head>

<body>
    {{#pages}}
        <div class="page">
            {{#panels}}
                <div class="panel">
                    {{#filename}}
                        <div class="handle">
                            {{filename}} | Page {{page}}
                        </div>
                        <div class="content" style="background-image:url({{filename}})"></div>
                    {{/filename}}
                </div>
            {{/panels}}
        </div>
    {{/pages}}
</body>

</html>
